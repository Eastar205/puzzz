# Agent Log

Claude Code, 다른 보조 도구 등 이 프로젝트를 건드리는 모든 작업 도구는 작업 시작 전에 이 파일을 먼저 읽고, 작업이 끝나면 아래 형식으로 새 항목을 **맨 위에** 추가한다. 코드 diff만으로는 "왜" 바꿨는지 알기 어려우니, 의도와 배경 위주로 짧게 남긴다.

## 형식
```
## YYYY-MM-DD - 도구명
- 무엇을 했는지 (건드린 파일/기능 위주, 간단히)
- 왜 했는지 (요청 배경, 설계 의도)
- 다음에 참고할 점 (있으면)
```

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - 24인의 영웅들 각각에 대해 **5가지 주요 모션(대기, 준비자세, 타격/공격, 회복, 기절/HP 0)**에 최적화된 개별 이미지 AI 생성용 영문 프롬프트 구성 완료.
  - [docs/CharacterPromptSheet.md](file:///d:/작업폴더/03.AI/작업용 폴더/Puzzz/docs/CharacterPromptSheet.md) 파일에 해당 5개 액션 모션별 영문 텍스트 세트를 2등신, 모바일 스타일, 좌향 조건과 결합하여 완전히 새로 갱신함.
- 왜 했는지:
  - 게임 개발 시 영웅들의 상태 변화(대기상태, 스킬 시전 대기, 공격 타격 순간, 힐링 회복 및 사망 상태)에 맞춘 리소스를 이미지 생성 AI로부터 정확하고 정밀한 자세(Pose)로 확보하기 위해.
- 다음에 참고할 점:
  - 각 모션 프롬프트는 2등신 SD 및 캐주얼 모바일 게임 에셋 스타일로 가공되어 있으므로, 5개 스프라이트 시트를 결합해 Unity 엔진 내에서 프레임 바이 프레임(Frame-by-frame) 애니메이션 혹은 UI 연출 컷씬으로 교환해 활용하기 적합함.

---

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - 이미지 생성 AI가 2등신 SD 비율과 모바일 게임 텍스처 스타일로 몬스터 방향인 왼쪽을 마주 보고 파츠를 분리하도록 **2등신 좌향 파츠 분할형 프롬프트([CharacterPromptSheet.md](file:///d:/작업폴더/03.AI/작업용 폴더/Puzzz/docs/CharacterPromptSheet.md))**로 전면 갱신.
  - 모든 프롬프트에 `chibi 2-head-tall style`, `casual mobile game asset`, `facing left` 지시어 추가 이식.
- 왜 했는지:
  - 횡스크롤 디펜스 게임상 우측에서 적들이 다가오기 때문에 모든 영웅이 왼쪽(적 방향)을 바라보고 수비 전투 모션을 취해야 하며, 2등신 캐주얼 화풍으로 일관성 있게 리소스를 확보하기 위해.
- 다음에 참고할 점:
  - DALL-E 3나 Midjourney에서 간혹 `facing left`를 거울 반전(`mirroring`)처럼 처리하여 무기 손 위치가 헷갈려 생성되는 경우가 있으니, 필요시 그래픽 편집기에서 가로 반전(Flip Horizontal)을 하여 대칭을 맞추어 쓸 것을 권장.

---

## 2026-07-16 - Claude Code
- 무엇을 했는지:
  - `web_demo.html`/`app_shell.html` 둘 다에서 헤더(`Puzzz 프로토타입` 타이틀 + 자동/수동 토글)와 웨이브 배너 행을 완전히 제거. 대신 `battle-ground` 내부 상단에 `.field-wave-tag`(웨이브 숫자)와 `.buff-banner`(보라 버프 표시)를 오버레이 배지로 배치
  - 헤더/웨이브 배너가 차지하던 공간이 사라지면서 `battle-area`/`board-wrap`이 기존 1:2 비율 그대로 더 커짐 (app_shell 기준 전장 128→158px, 퍼즐 394→453px)
  - **수동 스킬 발동 UI 완전 삭제**: `skillMode`를 `let`에서 `const "auto"`로 고정, 모드 토글 버튼/`setSkillMode`/카드 클릭 캐스팅/`ready` 상태(및 관련 CSS pulse 애니메이션) 전부 제거. 마나가 100 차면(힐러는 `canHealerAutoCast` 조건 충족 시) 무조건 즉시 `castSkill` 자동 발동
- 왜 했는지: "헤더+웨이브배너 없애고 그만큼 전장을 키워달라, 웨이브 정보는 전장 내부 상단에만 표시, 스킬은 항상 자동 발동"이라는 요청
- 다음에 참고할 점: 골드 표시(`#goldDisplay`)는 이제 카드에 `position:absolute`로 우상단에 떠 있음 (`.card`에 `position:relative` 추가함). 수동 모드 코드가 통째로 빠졌으니, 나중에 수동 발동을 다시 원하면 이 커밋 이전 버전(`git diff`/`git log`)을 참고해서 되살려야 함

## 2026-07-16 - Claude Code
- 무엇을 했는지: `web_demo.html`/`app_shell.html`의 `.battle-area`/`.board-wrap` flex 비율을 **2:1(전장 크게) → 1:2(퍼즐 크게)**로 재조정
- 왜 했는지: 직전 턴에 전장을 키우고 퍼즐을 줄였는데, 사용자가 유사 장르 레퍼런스 스크린샷(퍼즐앤드래곤류 등)을 보여주며 "퍼즐칸 비중이 너무 적다"고 지적. 레퍼런스들은 공통적으로 전장은 상단에 작게, 퍼즐 보드가 화면 대부분(대략 절반 이상)을 차지하는 구조였음
- 다음에 참고할 점: 비율 수치(`flex: 1 1 0%` / `flex: 2 1 0%`)는 두 값의 상대 비율만 의미 있음 — 추가로 조정이 필요하면 이 두 숫자만 바꾸면 됨. 실측: app_shell 기준 전장 128px vs 퍼즐 394px

---

## 2026-07-16 - Claude Code
- 무엇을 했는지:
  - `web_demo.html`/`app_shell.html` 둘 다에 **골드 드랍** 추가: 몬스터 처치(기본 공격/스킬 무관) 시 `max(3, maxHp*0.15)` 골드 획득, 상단에 🪙 표시(획득 시 살짝 튀는 애니메이션). 골드는 전투 재시작(`resetGame`)에도 초기화되지 않는 메타 재화로 취급
  - 전장(`battle-area`/`battle-ground`) : 퍼즐 보드 비율을 **2 : 1**로 조정. `.card`/`#screen-battle`를 뷰포트 높이(100dvh)에 맞춰 flex-column으로 채우고, `battle-area`는 `flex:2`, 퍼즐은 `.board-wrap`(`flex:1`)으로 감싸 `board`가 `height:100%; width:auto; aspect-ratio:1`로 자기 컨테이너 높이에 맞춰 정사각형을 유지하도록 변경 (기존엔 보드가 카드 전체 너비를 차지해 전장보다 훨씬 컸음)
- 왜 했는지: "몬스터 잡으면 골드 드랍", "전장 비율을 늘리고 퍼즐을 줄여달라"는 요청. "드래그 같은 요소없이 화면 비율에 맞게"는 사용자 확인 없이 유추한 것으로, 게임 플레이용 드래그(퍼즐 체인 연결)를 없애라는 뜻이 아니라 수동 리사이즈 UI 없이 CSS flex 비율만으로 화면 크기에 자동으로 맞추라는 의미로 해석해 진행함 — 이견 있으면 알려달라고 해야 함
- 다음에 참고할 점: 골드는 아직 실제 소비처(상점 구매 로직)와 연결 안 됨, `gold` 변수만 누적됨. `#screen-battle`의 세로 꽉 채우기는 `:has()` CSS 선택자를 사용하므로 구형 브라우저에서는 깨질 수 있음(최신 Chromium 계열만 확인함)

---

## 2026-07-16 - Claude Code
- 무엇을 했는지: `Prototype/app_shell.html` 신규 작성 — 로비/캐릭터·인벤토리/전투/상점/도감 5개 화면 + 하단 GNB, 우측 상단 우편함·공지·출석부 아이콘(알림 배지) 구조 추가. 기존 `web_demo.html`의 전투 화면(CSS+JS 전부)을 그대로 "전투" 탭 패널로 이식해 하나의 파일로 통합
  - 로비: 대표 캐릭터(전사) 아이콘, 순환 이벤트 배너, 시즌패스/일일퀘스트 단축 버튼, "전투 시작" CTA
  - 캐릭터/인벤토리: [캐릭터|장비|강화] 하위 탭. 캐릭터 탭은 4명 스탯 리스트, 장비/강화는 아직 시스템이 없어 "준비 중" 플레이스홀더로 정직하게 표시
  - 상점: 재화 패키지/소모품/스킨 3개 섹션 (뽑기와 분리), 구매 버튼은 토스트 메시지만 표시 (실제 결제 없음)
  - 도감: 보유 캐릭터 4종 + 아트가 있는 몬스터(고블린/임프)는 공개, 아트 없는 몬스터(오크/오크 대장)는 "?" 잠금 표시. 상단에 수집 진행도/보너스 스탯 요약
- 왜 했는지: 사용자가 제공한 모바일 RPG UI 참고자료(로비/캐릭터/상점/도감 + 하단 GNB + 우상단 알림 아이콘)를 바탕으로 게임 전체 UI 구조를 잡아달라는 요청
- 다음에 참고할 점: 상점/장비/강화/컬렉션 보너스는 전부 목업(mock) 상태 — 실제 재화·인벤토리 데이터 모델은 아직 없음. 오크·오크 대장 아트가 추가되면 도감에 자동 반영됨 (`MONSTER_ICON_DEFS`에 등록하면 됨)

---

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - 2D 스파인(Spine) 및 Live2D 리깅과 파츠 분할 애니메이션 적용이 가능하도록 **애니메이션 파츠 분할형 프롬프트([CharacterPromptSheet.md](file:///d:/작업폴더/03.AI/작업용 폴더/Puzzz/docs/CharacterPromptSheet.md))**로 전격 업데이트.
  - 모든 프롬프트에 `separated body parts layout, split into head, torso, separate arms, separate legs, weapons isolated on the side`와 같은 부위 분리용 인벤토리 템플릿 키워드 이식.
- 왜 했는지:
  - 캐릭터 일러스트를 한 장의 통짜 이미지가 아닌 머리, 몸통, 관절, 무기가 개별 분리된 상태로 생성하여, 게임 내에서 직접 움직임을 주는 리깅 애니메이션 작업에 즉시 쓸 수 있게 하기 위해.
- 다음에 참고할 점:
  - AI 툴에 따라 `spritesheet layout` 키워드가 겹쳐서 기괴하게 그려지는 경우, 일부 부위를 부분 프롬프트로 나누거나 `negative prompt`에 `combined body` 등을 넣으면 퀄리티가 정밀해짐.

---

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - 무협, 판타지, 그리스/북유럽 신화, 역사 과거, SF 미래 등 장르에 국한되지 않는 퓨전 세계관 기반의 **24인 캐릭터 설정 시트([CharacterSheet.md](file:///d:/작업폴더/03.AI/작업용 폴더/Puzzz/docs/CharacterSheet.md))** 작성.
  - 전사(탱커) / 마법사(누커) / 궁수(속사) / 힐러(유틸) 직군별로 6명씩 밸런스를 배정하였으며 종족, 성별, HP, 공격력, 공격속도, 액티브 스킬 기획 포함.
- 왜 했는지:
  - 현재 퍼즐 디펜스의 틀이 잡힌 상황에서 추후 영웅 수집/성장 콘텐츠 확장을 도모하기 위해 다양성 높고 매력적인 영웅 밸런스 풀을 미리 구상하기 위해.
- 다음에 참고할 점:
  - 설정 시트의 스펙은 2D 디펜스 프로토타입 게임 루프의 기준 스펙에 맞춰 정규화되어 있으며, 신규 영웅의 C# 데이터 스크립터블 오브젝트(ScriptableObject) 생성 시 이 시트의 속성 값들을 그대로 매핑할 수 있음.

---

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - 캐릭터들의 **마나 초과 축적 및 이월 시스템**을 개발. 마나가 100을 넘어 계속 누적되도록 한계 한도를 해제하고, 스킬 시전 시 마나가 0으로 초기화되는 대신 정확히 소모량 **100**만 차감하고 남은 마나가 이월되도록 구현.
  - 마나가 100을 초과하여 축적되는 수치를 영웅 카드 우측에 `MP:124` 형태로 실시간 표기 처리.
  - 자동(Auto) 모드 시 **힐러의 스마트 캐스팅 인공지능**을 이식. 마나가 100이 넘더라도, 현재 수비 중인 영웅의 손실 체력이 힐량(기본 40, 오버체인 80) 이상으로 닳아 회복량이 낭비 없이 100% 채워질 수 있는 타이밍에만 자동으로 스킬을 사용하도록 조율.
- 왜 했는지:
  - 10체인 이상의 오버체인 연결이나 자연 회복이 겹쳐 마나가 100 이상으로 획득되는 초과량이 버려지지 않게 방지하여 연속 스킬 활용의 재미를 높이고, 자동 모드 힐러가 체력이 가득 찬 상황에 힐 스킬을 낭비하는 문제를 해결하기 위해.
- 다음에 참고할 점:
  - 수동(Manual) 모드에서는 힐 효율 조건과 관계없이 유저가 카드를 클릭하면 즉시 힐을 시전합니다.

---

## 2026-07-16 - Claude Code
- 무엇을 했는지:
  - `Assets/Art/Characters/archer_green.svg`, `healer_yellow.svg`를 `archer_yellow.svg`, `healer_green.svg`로 재작성(파일명·배경 그라디언트 색만 교체, 글리프는 동일) — 캐릭터 색상 매핑이 궁수=노랑/힐러=초록으로 바뀐 뒤 아트 파일 색이 안 맞았음
  - `Prototype/web_demo.html`에 `CHAR_ICON_DEFS`/`MONSTER_ICON_DEFS` + `characterIconSvg()`/`monsterIconSvg()` 헬퍼를 추가해 기존 이모지(🛡️🧙🏹🌿, 👺😈)를 실제 SVG 원화로 교체. 전장의 캐릭터 4명, 하단 캐릭터 카드 아이콘, 고블린/임프 몬스터에 적용. 오크·오크 대장은 아직 원화가 없어 이모지 폴백 유지
- 왜 했는지: 지난 세션에 만들어둔 임시 아트웍을 실제로 화면에 입혀달라는 요청
- 다음에 참고할 점: 오크(👿)/오크 대장(💀) SVG 아이콘은 아직 없음. 추가하면 `MONSTER_ICON_DEFS`에 라벨 키로 등록만 하면 자동 적용됨

---

## 2026-07-16 - Claude Code
- 무엇을 했는지: `Prototype/web_demo.html`의 `.field-characters` CSS를 `flex-direction: row-reverse`로 변경
- 왜 했는지: 캐릭터 데이터 순서(`CHARACTERS` 배열: 전사→마법사→궁수→힐러)와 전투 로직(`getFrontlineAlive`)은 이미 전사가 최전방이었는데, 화면 렌더링 순서는 `flex-direction: row`라서 DOM 순서 그대로 왼쪽부터 전사가 그려져 있었음. 몬스터는 오른쪽에서 다가오므로 실제로는 힐러(맨 오른쪽)가 몬스터와 가장 먼저 마주치는 것처럼 보이는 문제였음. 사용자가 "전사가 맨앞에서 몬스터를 대치하도록" 요청해서 발견/수정
- 다음에 참고할 점: 데이터 순서(배열 인덱스)와 화면 시각 순서가 별개로 관리되고 있으니, 진형 순서를 다시 바꿀 일이 있으면 `CHARACTERS` 배열뿐 아니라 `.field-characters`의 `flex-direction`(현재 row-reverse)도 같이 확인할 것

---

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - 10체인 이상 드래그 연결 후 해제 시 발동하는 **오버체인(Overchain) 시스템**을 개발. 오버체인 시 캐릭터 스킬 데미지와 힐 스킬 효과가 **2배**(데미지 120, 힐 80)로 증폭되며, 보라색 버프는 공격 속도가 **4배**로 적용되도록 구현.
  - 10체인 이상 성공 시 인게임 중앙에 "💥 오버체인 발동! 💥" 피드백 메시지가 표시되도록 설정.
  - 캐릭터들의 마나가 자동으로 서서히 회복되도록 **자연 마나 리젠(초당 2.0 회복)** 기능을 추가하여 퍼즐 조작이 늦더라도 점진적인 대처가 가능하게 조율.
- 왜 했는지:
  - 퍼즐을 대량으로 이었을 때의 고점 쾌감(오버체인 한방딜)을 선사하고, 퍼즐 매칭에 서툰 순간에도 스킬을 점진적으로 사용할 수 있게 자동 구제 시스템을 겸비하기 위해.
- 다음에 참고할 점:
  - 대각선 판정과 자동 마나 회복이 겹쳐 마나 수급이 이전보다 원활해졌으므로, 몬스터들의 웨이브별 체력 스펙을 차후 더 높이거나 스포너를 강화할 수 있는 여지를 열어두어야 함.

---

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - 몬스터들의 기본 공격 데미지를 **8**에서 **18**로 상향하여 전사가 피격되기 시작할 때의 긴장감을 증폭.
  - 몬스터 스폰 간격을 1.8초에서 **1.5초**로 축소하여 라인 돌파 시도를 더 빈번하게 유도.
  - 고블린(HP 40 -> 95), 임프(HP 50 -> 120), 오크(HP 80 -> 240), 오크 대장(HP 140 -> 550)으로 전체 체력을 대폭 높여 스킬 점사를 고민하도록 밸런싱.
- 왜 했는지:
  - 전사(HP 200)가 쓰러지기 전에 옐로 구슬을 연결하여 힐을 주어야 할지, 아니면 타 속성 공격 구슬을 연결해 폭딜로 녹여야 할지에 대한 영리한 기획적 의도(퍼즐 대처 재미)를 강화하기 위해.
- 다음에 참고할 점:
  - 힐 스킬 회복량(40) 대비 몬스터 1마리당 가하는 피해가 18이므로, 2마리 이상의 적이 겹쳐서 전사를 타격할 경우 힐 사이클이 늦어지면 위험해짐.

---

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - 몬스터들이 한꺼번에 뭉탱이로 뭉쳐 나와 겹치는 현상을 해결하기 위해 몬스터별 1.8초의 시간차 스폰(딜레이 오프셋) 개념을 도입.
  - 필드 렌더러와 타겟팅 연산에서 아직 대기 상태인(progress < 0) 몬스터는 노출 및 타겟 지정에서 완전히 제외하고 0이 넘은 순간부터 필드에 순서대로 입장시킴.
  - 몬스터들의 Y 오프셋을 제거(0px 고정)하여 꼬리를 무는 아름다운 가로 한 줄 대열로 기지에 다가오게 수정.
- 왜 했는지:
  - 적들이 여러 마리 뭉쳐서 움직여 비주얼이 깨지고 동시 처치되는 문제를 방지하고, 명확히 꼬리를 물고 다가오는 한 줄 라인 디펜스의 느낌을 구현하기 위해.
- 다음에 참고할 점:
  - 몬스터 스폰 시간 오프셋(1.8초)은 추후 기획 밸런스에 따라 딜레이 수치를 쉽게 조절 가능함.

---

## 2026-07-16 - Antigravity
- 무엇을 했는지:
  - `Prototype/web_demo.html`의 몬스터 웨이브 배틀 패널을 좌(기지) - 중(영웅 가로 수비선) - 우(몬스터 스폰 및 이동) 구조의 실시간 횡스크롤 디펜스 전장으로 개편.
  - 구슬 드래그 연결 판정을 상하좌우에서 8방향 대각선 매칭까지 가능하게 확장.
  - 전사(빨강), 마법사(파랑), 궁수(노랑), 힐러(초록)의 밸런스 속성 및 색상을 완벽하게 동기화.
  - 전투 시작 전 대기할 수 있는 일시정지("⚔️ 디펜스 시작" 버튼 및 오버레이) 기능 및 투사체 발사/몬스터 피격 연출을 보완.
  - Unity C# 스크립트(`WaveManager.cs`, `Monster.cs`, `WaveDefinition.cs`)에 라벨, 이동속도 배율, 동시 스폰 처리 및 거리 기반 타겟팅 로직 등을 함께 설계하여 이식.
- 왜 했는지:
  - 사용자가 기획한 "몬스터가 오른쪽에서 밀려오고 캐릭터들이 가로로 배치되어 이를 방어하는 웨이브 디펜스" 레이아웃을 구현하고, 단조로운 퍼즐 연결을 대각선 매칭으로 다채롭게 확장하여 타격감과 편의성을 극대화하기 위해.
- 다음에 참고할 점:
  - 웹 데모 수준에서 디펜스 필드와 캐릭터 밸런스를 확인했으므로, 향후 Unity Scene 구축 시 2D Sprite 배치 및 Wave 스폰 로직을 웹 데모 기획에 맞춰 일관성 있게 조율해야 함.

---

## 2026-07-16 - Claude Code
- Unity 프로젝트 골격 생성 (`Assets/Scripts`, `Assets/Resources`, `Packages/manifest.json`, `ProjectSettings` 등), 게임 컨셉 문서(`docs/GameConcept.md`) 작성
- 컨셉이 세 번 크게 바뀜: (1) 캐주얼 매치 퍼즐(턴제, 탭 매치) → (2) 7x7 드래그 체인 매치 → (3) "퍼즐형 캐릭터 디펜스" (하단 퍼즐로 상단 캐릭터 마나/스킬을 충전해 몬스터를 막는 하이브리드). 그때마다 이전 방식 스크립트/스테이지 데이터는 삭제하고 새로 작성
- 핵심 스크립트: `Puzzle/PuzzleBoard.cs`(무한 리필형 보드), `Puzzle/ChainRules.cs`(인접 판정), `Puzzle/ManaFormula.cs`(마나/버프 계산식), `Battle/Character.cs`, `CharacterRoster.cs`, `Monster.cs`, `WaveManager.cs`, `BattleManager.cs`, `Core/GameManager.cs`
- 사용자가 별도 도구로 `WaveManager.cs`/`WaveDefinition.cs`/`Monster.cs`/`wave_01.json`을 직접 수정(라벨/이동속도 배율 필드 추가, 몬스터 동시 스폰 지원, 웨이브 자동 진행 로직 정교화) — 이 변경을 반영해서 이후 작업 진행
- 캐릭터 밸런스 확정 및 전투 방식을 "라인 디펜스"로 변경: 전사(빨강, HP200, 근접 탱커)가 맨 앞에서 막고 쓰러지면 마법사(파랑, 느림·강함) → 궁수 → 힐러 순으로 다음 캐릭터가 피격 대상이 됨. 진형 전멸 후 몬스터가 기지에 도달하면 **즉시 게임오버** (기존 기지 체력 풀 방식 삭제). 힐러 스킬은 "기지 회복"에서 "현재 피격 중인 캐릭터 회복"으로 변경
- `Assets/Art/Characters/`, `Assets/Art/Monsters/`에 캐릭터 4종·몬스터 2종 SVG 실루엣 아이콘 제작 (플레이스홀더, 추후 실제 일러스트로 교체 예정)
- `Prototype/web_demo.html`: 사용자가 직접 가로 전장(우→좌 이동, 다중 몬스터 동시 진격) 비주얼로 재작성한 버전 위에, 위 라인 디펜스 전투 로직(캐릭터별 스탯, 진형 타겟 전환, 즉시 패배)을 다시 병합
- **다음에 참고할 점**: `Assets/Resources/Waves/wave_02.json`, `wave_03.json`에는 아직 안 쓰는 `baseHp` 필드가 남아있음 (코드에서는 제거됨, JsonUtility가 무시하긴 하지만 정리 필요). 아직 Unity Scene/Prefab은 실제로 만들어진 게 없어서 에디터에서 프리팹 생성·씬 배치가 남은 수동 작업임 (`README.md` 참고)
